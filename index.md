`legacy-version`

- stored lyrics when in the legacy version, there is no song section `---- ` and page breaker `**` and empty slide `***`

`version-1`

- song section `---- ` is introduced

`version-2`

- page breaker `**` and empty slide `***` is introduced

`version-3`

- metadata `@` and inline settings overwrites `{}` is introduced
